```##Log File Scanner```

A Python script that scans log files for common error, warning, and failed messages. It performs a case-insensitive search and displays the matching lines along with their line numbers.
